# 2026-06-04 — Session Log


## User (turn 0)

what is the status of 2026-05-30-full-kde-experience ?

## User (turn 2)

yes

## User (turn 3)

what is the status of 2026-05-30-code-review-fixes ?

## User (turn 4)

what is the status of 2026-05-30-code-review-fixes.md ?

## User (turn 5)

yes, kick off the anvil build in the background

## User (turn 6)

sweep all plans and update status. update TODO with any plans still not implemented as well as deferred or punted (or forgotten!?) tasks from plans already implemented

## User (turn 7)

do both

## User (turn 8)

do create-foundry-usb

## User (turn 9)

https://github.com/foundry-linux/foundry-apt

Vendored upstream packages
Packages not in the Ubuntu archive, repackaged and shipped from this repo via dpkg-buildpackage using the canonical Debian source-package layout:

Package    Upstream    Notes
f9dasm    Arakula/f9dasm    Motorola 6800/6809/6309 family disassembler. amd64.

pretty sure we're vendoring more upstream packages than that

## User (turn 10)

update 2026-05-29-state-of-the-distro

## User (turn 11)

yes

## User (turn 12)

"  Nothing else references the old single-row list — LICENSES-VENDORED.md is linked, not duplicated. Want me to cross-check that
  file has all 14 too, and commit?"

yes

## User (turn 13)

i want to consider what we can *not* put on the usb stick if/when it's <4 gb

## User (turn 14)

i want to consider what we can *not* put on the usb stick if/when it's 4 gb or less

## User (turn 15)

* check Qt WebEngine to see if it's actually used or not (and even if it's not used, is this something we should still be distributing for other applications??)
* i also want to know what 2gb and 1gb usb stick could look like
* give me some bar charts or other graphs/charts to help visualize
* sort our potential cut list by size DESC in general

## User (turn 16)

put all of this stuff on the plan, ofc

## User (turn 17)

update 2026-05-29-state-of-the-distro

## User (turn 18)

i don't want a super slim edition; i want the most that will fit on a 4gb stick

## User (turn 19)

continue

## User (turn 20)

yes

## User (turn 21)

update docs

## User (turn 22)

deal with 3 generated site artifacts to gitignore, the .history/CLAUDE.md straggler

"Dropped without follow‑through: Steam/Sniper release containers, per‑game Distrobox, gamescope kiosk,"
umm, we still want those things

## User (turn 23)

fix
BUG: snapd ships despite the strip-list — snapd snap-store is in strip.list.chroot.purge + hook 0020, but snapd (140 MiB) is still in foundry-anvil-0.9.36. Likely re-pulled as a Recommends after the purge pass / autoremove not catching it. ~140 MiB free across every edition once fixed.

## User (turn 24)

foundry-setup gap

## User (turn 25)

3. Track the separate distribution gap (do NOT solve here)
Exploration surfaced a second, distinct gap: foundry-setup/README.md tells users to run curl -fsSL https://foundrylinux.org/install.sh | bash, but site/install.sh does not exist (the site only serves site/setup.sh, the apt-source wiring script), and install.sh isn't self-contained anyway — it's a local dispatcher that sources lib.sh + sibling install-foundry-*.sh. So Phase 0's full installer has no working public curl-bash path. This needs a product decision (serve a self-contained installer? document git-clone/tarball? rely on setup.sh + apt only?) and is out of scope for the CI fix.



let's deal with this

## User (turn 26)

3. Track the separate distribution gap (do NOT solve here)
Exploration surfaced a second, distinct gap: foundry-setup/README.md tells users to run curl -fsSL https://foundrylinux.org/install.sh | bash, but site/install.sh does not exist (the site only serves site/setup.sh, the apt-source wiring script), and install.sh isn't self-contained anyway — it's a local dispatcher that sources lib.sh + sibling install-foundry-*.sh. So Phase 0's full installer has no working public curl-bash path. This needs a product decision (serve a self-contained installer? document git-clone/tarball? rely on setup.sh + apt only?) and is out of scope for the CI fix.



let's deal with this

## User (turn 27)

why does quick startup recommend

sudo apt update
sudo apt install foundry-retro-tools

?

## User (turn 28)

<task-notification>
<task-id>b6upuw6qd</task-id>
<tool-use-id>toolu_01S15CRkRz7mLNusMmjrJ57d</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/3ad1b396-3147-424b-94df-88c36b277ef5/tasks/b6upuw6qd.output</output-file>
<status>completed</status>
<summary>Background command "Run full engine-dev install end-to-end in container" completed (exit code 0)</summary>
</task-notification>

## User (turn 29)

● Excellent progress — the install cleared the gpg step (my fix worked) and is now doing the real apt install of engine-build-deps
  from the live resolute repo. The debconf: Dialog frontend will not work lines are harmless (it falls back to Noninteractive),
  though setting DEBIAN_FRONTEND=noninteractive would quiet them.

  The full install is downloading packages in the background (task b6upuw6qd) — it'll notify me when done rather than me polling
  with sleep. Where things stand:

  ┌─────────────────────────────────────────────────────────────┬──────────────────────────────────────────────────────────────┐
  │                        Cascade step                         │                            Status                            │
  ├─────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
  │ 1. sudo -v → non-interactive check                          │ ✅ fixed + committed                                         │
  ├─────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
  │ 2. gpg missing on minimal base                              │ ✅ fixed (ensure-gnupg in both apt-source scripts) —         │
  │                                                             │ confirmed past it                                            │
  ├─────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────┤
  │ 3. real apt install (build-deps → task → retro-tools from   │ ⏳ running now                                               │
  │ live repo)                                                  │                                                              │
  └─────────────────────────────────────────────────────────────┴──────────────────────────────────────────────────────────────┘

## User (turn 30)

● All source changes and docs are complete and internally consistent. Here's where things stand — I'm holding the commit until the
  ISO build proves the result.

  Done (this session):
  - Metapackages — foundry-retro-tools 1.0.7 (ghidra removed, MAME kept — verified via dpkg-deb), foundry-atelier 0.9.2 (ghidra
  added), foundry-core 1.0.3 (desc). Built into dist/ (retro-tools via container due to root-owned .debhelper cruft).
  - ISO — plasma-workspace-wallpapers added to the strip list (the only safe free strip; breeze-wallpaper and firmware would
  cascade, so skipped); new hook 1010-trim-atelier-only-pkgs dependency-drives ghidra+JDK out of anvil/sprite but keeps them on
  atelier. snapd already handled by your no-snapd.pref pin.
  - Docs — CLAUDE.md (edition table + retro-tools line), foundry-apt/README (category + atelier tables), the usb-sizing plan
  (pivoted SLIM → "trim anvil, ghidra→atelier"), scenario chart regenerated (anvil NEW ~3.3 vs OLD 4.49), TODO reconciled.
  Historical 2026-05-xx plan records left untouched (they're records, not live docs).
  
  - Metapackages — foundry-retro-tools 1.0.7 (ghidra removed, MAME kept — verified via dpkg-deb), foundry-atelier 0.9.2 (ghidra
  added), foundry-core 1.0.3 (desc). Built into dist/ (retro-tools via container due to root-owned .debhelper cruft).
  - ISO — plasma-workspace-wallpapers added to the strip list (the only safe free strip; breeze-wallpaper and firmware would
  cascade, so skipped); new hook 1010-trim-atelier-only-pkgs dependency-drives ghidra+JDK out of anvil/sprite but keeps them on
  atelier. snapd already handled by your no-snapd.pref pin.
  - Docs — CLAUDE.md (edition table + retro-tools line), foundry-apt/README (category + atelier tables), the usb-sizing plan
  (pivoted SLIM → "trim anvil, ghidra→atelier"), scenario chart regenerated (anvil NEW ~3.3 vs OLD 4.49), TODO reconciled.
  Historical 2026-05-xx plan records left untouched (they're records, not live docs).

  In flight: the EDITION=anvil ISO build (now 0.9.37) is mid package-install; hooks 0020/0030/1000/1010 run after that. I'll get a
  completion notification.

  Holding commit until the build verifies:
  1. anvil ISO ≤ 3.6 GiB
  2. ghidra + openjdk-21-* absent from anvil's squashfs; MAME/ScummVM/Blender/VTK/snapd-absent confirmed
  3. (the 1010 hook actually trims the transient ghidra — its logic is sound but unproven on a real build)

  If the build comes back clean I'll commit the whole set with the evidence pasted into the plan's verification section. If hook
  1010 doesn't fully clear ghidra, I'll fix forward before committing. I'll also regenerate the manifest appendix from the new ISO
  at that point.

  One note: foundry-setup/setup-*-apt-source.sh show as modified but aren't mine — I'll leave them unstaged.

## User (turn 31)

● All source changes and docs are complete and internally consistent. Here's where things stand — I'm holding the commit until the
  ISO build proves the result.

  Done (this session):
  - Metapackages — foundry-retro-tools 1.0.7 (ghidra removed, MAME kept — verified via dpkg-deb), foundry-atelier 0.9.2 (ghidra
  added), foundry-core 1.0.3 (desc). Built into dist/ (retro-tools via container due to root-owned .debhelper cruft).
  - ISO — plasma-workspace-wallpapers added to the strip list (the only safe free strip; breeze-wallpaper and firmware would
  cascade, so skipped); new hook 1010-trim-atelier-only-pkgs dependency-drives ghidra+JDK out of anvil/sprite but keeps them on
  atelier. snapd already handled by your no-snapd.pref pin.
  - Docs — CLAUDE.md (edition table + retro-tools line), foundry-apt/README (category + atelier tables), the usb-sizing plan
  (pivoted SLIM → "trim anvil, ghidra→atelier"), scenario chart regenerated (anvil NEW ~3.3 vs OLD 4.49), TODO reconciled.
  Historical 2026-05-xx plan records left untouched (they're records, not live docs).
  
  - Metapackages — foundry-retro-tools 1.0.7 (ghidra removed, MAME kept — verified via dpkg-deb), foundry-atelier 0.9.2 (ghidra
  added), foundry-core 1.0.3 (desc). Built into dist/ (retro-tools via container due to root-owned .debhelper cruft).
  - ISO — plasma-workspace-wallpapers added to the strip list (the only safe free strip; breeze-wallpaper and firmware would
  cascade, so skipped); new hook 1010-trim-atelier-only-pkgs dependency-drives ghidra+JDK out of anvil/sprite but keeps them on
  atelier. snapd already handled by your no-snapd.pref pin.
  - Docs — CLAUDE.md (edition table + retro-tools line), foundry-apt/README (category + atelier tables), the usb-sizing plan
  (pivoted SLIM → "trim anvil, ghidra→atelier"), scenario chart regenerated (anvil NEW ~3.3 vs OLD 4.49), TODO reconciled.
  Historical 2026-05-xx plan records left untouched (they're records, not live docs).

  In flight: the EDITION=anvil ISO build (now 0.9.37) is mid package-install; hooks 0020/0030/1000/1010 run after that. I'll get a
  completion notification.

  Holding commit until the build verifies:
  1. anvil ISO ≤ 3.6 GiB
  2. ghidra + openjdk-21-* absent from anvil's squashfs; MAME/ScummVM/Blender/VTK/snapd-absent confirmed
  3. (the 1010 hook actually trims the transient ghidra — its logic is sound but unproven on a real build)

  If the build comes back clean I'll commit the whole set with the evidence pasted into the plan's verification section. If hook
  1010 doesn't fully clear ghidra, I'll fix forward before committing. I'll also regenerate the manifest appendix from the new ISO
  at that point.

  One note: foundry-setup/setup-*-apt-source.sh show as modified but aren't mine — I'll leave them unstaged.

## User (turn 32)

#3 obvs

## User (turn 33)

the forge cards was correct (afaik).

the text i pasted was from README, actually

Quick start
curl -fsSL https://apt.foundrylinux.org/key.gpg \
  | sudo gpg --dearmor -o /etc/apt/keyrings/foundry.gpg
echo "deb [signed-by=/etc/apt/keyrings/foundry.gpg] https://apt.foundrylinux.org resolute main" \
  | sudo tee /etc/apt/sources.list.d/foundry.list
sudo apt update
sudo apt install foundry-retro-tools

## User (turn 34)

should the quick start pull in a whole desktop? 
who is the audience?  are they installing from an existing linux distro? or this one?

## User (turn 35)

<task-notification>
<task-id>b7vv5ib6d</task-id>
<tool-use-id>toolu_012UraKRBRcbdonoLWi58Bzp</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/3ad1b396-3147-424b-94df-88c36b277ef5/tasks/b7vv5ib6d.output</output-file>
<status>completed</status>
<summary>Background command "Run full install test against Ubuntu 26.04" completed (exit code 0)</summary>
</task-notification>

## User (turn 36)

commit this

## User (turn 37)

commit

## User (turn 38)

<task-notification>
<task-id>bxwd9ldkc</task-id>
<tool-use-id>toolu_01PFVFaYEsBC3T3QhMC8EgCo</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/00bb0cdc-4dd7-4365-b756-deb3f48fcb81/tasks/bxwd9ldkc.output</output-file>
<status>completed</status>
<summary>Background command "Build anvil ISO in background" completed (exit code 0)</summary>
</task-notification>

## User (turn 39)

update doc with sizes and packages and graphs

## User (turn 40)

continue

## User (turn 41)

continue

## User (turn 42)

<task-notification>
<task-id>b4amzj27j</task-id>
<tool-use-id>toolu_01H27TN7PLCmaL9t3WdGTBaA</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/00bb0cdc-4dd7-4365-b756-deb3f48fcb81/tasks/b4amzj27j.output</output-file>
<status>completed</status>
<summary>Background command "Build 0.9.38 ISO in background" completed (exit code 0)</summary>
</task-notification>

## User (turn 43)

<task-notification>
<task-id>burowyti1</task-id>
<tool-use-id>toolu_011RFHXzt9WAAhGPHzfkUjkw</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/00bb0cdc-4dd7-4365-b756-deb3f48fcb81/tasks/burowyti1.output</output-file>
<status>completed</status>
<summary>Background command "Build 0.9.39 ISO with corrected Mozilla pin (a=mozilla)" completed (exit code 0)</summary>
</task-notification>

## User (turn 44)

<task-notification>
<task-id>b581nul4p</task-id>
<tool-use-id>toolu_01XnB6CuEvnGqqUKsVq6Mmpk</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/00bb0cdc-4dd7-4365-b756-deb3f48fcb81/tasks/b581nul4p.output</output-file>
<status>completed</status>
<summary>Background command "Build 0.9.40 ISO — hook 0025 replaces firefox then purges snapd" completed (exit code 0)</summary>
</task-notification>

## User (turn 45)

<task-notification>
<task-id>bcyq1rp5q</task-id>
<tool-use-id>toolu_01KhDdS3CZ93aFKUBwgKyKDB</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/00bb0cdc-4dd7-4365-b756-deb3f48fcb81/tasks/bcyq1rp5q.output</output-file>
<status>completed</status>
<summary>Background command "Build 0.9.41 ISO — dpkg force-removes snapd then apt-get --fix-broken installs Mozilla firefox" completed (exit code 0)</summary>
</task-notification>

## User (turn 46)

<task-notification>
<task-id>be5hcx9m0</task-id>
<tool-use-id>toolu_01QLQRBQptWhpV91V75rFgUo</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/00bb0cdc-4dd7-4365-b756-deb3f48fcb81/tasks/be5hcx9m0.output</output-file>
<status>completed</status>
<summary>Background command "Build 0.9.42 — force-purge both snapd and snap-firefox together, then install Mozilla firefox" completed (exit code 0)</summary>
</task-notification>

## User (turn 47)

we're not getting rid of MAME, btw

we could move some of python game dev libraries to atelier, but i do still want to keep a base of python game dev stuff, but not some of the bigger and more esoteric stuff, like librosa could be split off (and others?)

## User (turn 48)

yes, and fix the firefox/snapd issue too

## User (turn 49)

we need 2 metapackages names now

## User (turn 50)

we need 2 metapackages names now for the python

## User (turn 51)

are there other computer vision packages (not python) that we could bundle together with the python version? what do you think?

## User (turn 52)

yes, include those three, 
also add gstreamer1.0-opencv

what's libonnxruntime?

## User (turn 53)

foundry-cv, and onnxruntime in extras yes

## User (turn 54)

just build it for real and check out the actual sizes, update the docs with the sizes, and then we will try to copy it to a 4gb usb stick :D

## User (turn 55)

"      465 +      - DEVICE="{{.DEVICE}}" EDITION="{{.EDITION | default "anvil"}}" bash scripts/write-usb.sh                  "

can we use the same core script that's used in the distro? that would be better testing ;-)

## User (turn 56)

"apt-get autoremove should then sweep out opencv + libvtk9.5"

why do we need to do this?
didn't we split them into different metapackages and thus simply not install them on anvil??

## User (turn 57)

<task-notification>
<task-id>bxg2cro21</task-id>
<tool-use-id>toolu_012NpKpa1FrPKQPTNaa3FEJG</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-SRC-foundrylinux-org/8ee2a7af-0bcb-4b3b-b629-c01a55a83c3b/tasks/bxg2cro21.output</output-file>
<status>completed</status>
<summary>Background command "Build anvil ISO (apt-build + iso-build, will take 20-40 minutes)" completed (exit code 0)</summary>
</task-notification>

## User (turn 58)

"  - Snapd removal: 5 build attempts all blocked — firefox 1:1snap1-0ubuntu8 has PreDepends: snapd, which overrides any pin
  priority; dpkg force-removal approaches abort the build. Documented and parked."

and there isn't extra cruft around taking space because of these attempts, right?


"4 GB stick target (3.72 GiB) not met."
well, we're still going to try copying it to a 4gb stick


"The remaining gap to ≤ 3.6 GiB requires cutting MAME or foundry-python-gamedev-extras"
i don't think that's strictly true, but let's set it aside until we run some tests
