{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"${CLAUDE_TOOL_INPUT_FILE_PATH:-}\" | grep -qE \"foundry-apt/packages/.+/(build\\.sh|debian/control)$\"; then bash foundry-apt/scripts/check-licenses-vendored.sh && bash foundry-apt/scripts/check-repology-badges.sh; fi'"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/check-todo-done.sh"
          }
        ]
      }
    ]
  }
}
